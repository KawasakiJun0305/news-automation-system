# Monetization Roadmap - マネタイズ基盤設計書

## 📋 概要

今は無料で使うが、**将来 Note 販売や API 化に対応できる基盤** を整備。

無理にマネタイズせず、「できるようにしておく」が目的。

---

## 🚀 フェーズ別ロードマップ

### **フェーズ 1（今）：基盤構築**
```
期間：1-3 ヶ月
目標：毎朝ニュースが自動配信される

実装内容：
- 基本的なニュース取得・配信
- HTML + Markdown + Notion 出力
- GitHub Pages 公開

マネタイズ：なし
```

### **フェーズ 2（3-6 ヶ月後）：データ蓄積**
```
期間：1 ヶ月
目標：過去データの蓄積開始

実装内容：
- SQLite / CSV にデータ保存
- 「過去 30 日のトレンド」を可視化
- 簡易分析機能

マネタイズ：検討段階
```

### **フェーズ 3（6-9 ヶ月後）：分析強化**
```
期間：2-3 ヶ月
目標：有料コンテンツになる品質の分析を生成

実装内容：
- 「AI 業界のトレンド分析」生成
- 企業別の露出度追跡
- ダッシュボード実装

マネタイズ：Note で試験販売開始
  例：月 ¥500 で「今月の AI トレンド分析」
```

### **フェーズ 4（9-12 ヶ月後）：拡張・マネタイズ**
```
期間：3-6 ヶ月
目標：複数の収益源を確立

オプション A：Note マガジン化
  - 月 ¥500-1,000
  - 予想月収：¥5,000-50,000（購読者次第）

オプション B：法人向けカスタマイズ
  - 企業向けに特化版を提供
  - 月 ¥50,000-500,000

オプション C：API 化 / SaaS
  - 他のユーザーが使える形に
  - 月額課金型
```

---

## 💾 データ蓄積戦略

### テーブル設計（SQLite）

```sql
-- articles_archive テーブル
CREATE TABLE articles_archive (
    id TEXT PRIMARY KEY,
    title TEXT,
    category TEXT,
    relevance_score INTEGER,
    published_at DATETIME,
    fetched_at DATETIME,
    source_name TEXT,
    summary TEXT,
    keywords TEXT,  -- JSON
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 検索用インデックス
CREATE INDEX idx_category_date 
ON articles_archive(category, published_at DESC);
```

### 集計用ビュー

```python
# src/storage/analytics.py

class AnalyticsEngine:
    """
    蓄積データから分析を生成
    """
    
    def get_category_stats(self, category: str, days: int = 30) -> dict:
        """
        カテゴリ別の統計情報
        """
        return {
            'total_articles': 150,
            'avg_score': 68,
            'trending_keywords': ['AI', '企業', '発表'],
            'top_sources': ['TechCrunch', 'Nature']
        }
    
    def get_company_mentions(self, company: str, days: int = 30) -> list:
        """
        企業の露出度追跡
        """
        return [
            {'date': '2026-01-29', 'count': 5},
            {'date': '2026-01-28', 'count': 3},
        ]
    
    def generate_trend_report(self, category: str) -> str:
        """
        カテゴリ別のトレンドレポート（有料コンテンツ化可能）
        """
        stats = self.get_category_stats(category)
        
        report = f"""
【{category}業界 今月のトレンド】

記事数：{stats['total_articles']} 件
平均スコア：{stats['avg_score']}

トレンドキーワード：
{', '.join(stats['trending_keywords'])}

注目企業：
{', '.join(stats['top_sources'])}
"""
        return report
```

---

## 🎨 将来の API 化

### エンドポイント例

```python
# src/api/routes.py

from fastapi import FastAPI

app = FastAPI()

@app.get("/api/articles")
async def get_articles(category: str = None, days: int = 7):
    """
    過去の記事を JSON で取得
    """
    articles = fetch_articles(category, days)
    return {
        "status": "success",
        "count": len(articles),
        "articles": articles
    }

@app.get("/api/trends")
async def get_trends(category: str):
    """
    トレンド分析データを取得
    """
    analytics = AnalyticsEngine()
    trends = analytics.generate_trend_report(category)
    return {"trends": trends}

@app.get("/api/company/{company_name}")
async def get_company_info(company_name: str):
    """
    企業別の露出度・ニュース取得
    """
    ...
```

---

## 💳 ユーザー管理（将来用）

```python
# src/user_management/models.py

class UserPlan:
    """Free / Pro / Enterprise プラン"""
    
    FREE = {
        'price': 0,
        'api_calls_per_day': 100,
        'data_retention_days': 7,
        'features': ['基本検索']
    }
    
    PRO = {
        'price': '$5/month',
        'api_calls_per_day': 1000,
        'data_retention_days': 30,
        'features': ['トレンド分析', 'カスタマイズ']
    }
    
    ENTERPRISE = {
        'price': 'custom',
        'api_calls_per_day': 'unlimited',
        'data_retention_days': 'unlimited',
        'features': ['全機能', 'サポート']
    }
```

---

## 📊 想定市場

### ターゲット層

1. **個人**（あなた）
   - 現在：無料で使用
   - 将来：Note で月 ¥500 の有料記事購読

2. **営業・営業企画**
   - ニーズ：競合企業の動きを自動追跡
   - 想定月額：¥10,000-50,000

3. **投資家・アナリスト**
   - ニーズ：市場トレンドの自動分析
   - 想定月額：¥50,000-500,000

4. **大企業**
   - ニーズ：業界特化型カスタマイズシステム
   - 想定受託額：¥100,000-1,000,000

---

## 📈 成長シナリオ

```
【6 ヶ月目】
個人で毎日使用
  ↓

【12 ヶ月目】
Note で月 20 人購読 → 月 ¥10,000

  ↓

【18 ヶ月目】
営業向けカスタマイズ受託 3 案件
→ 月 ¥100,000

  ↓

【24 ヶ月目】
SaaS 化、ユーザー 50 人
→ 月 ¥250,000
```

---

## ⚠️ 注意点

1. **著作権**
   - ニュース記事をそのまま販売できない
   - 「あなたの分析・洞察」を付加価値に

2. **法務**
   - 個人事業主登録が必要になる可能性
   - 弁護士相談推奨

3. **競合**
   - 既存の News API サービスが多い
   - 差別化が重要

---

## 🎯 今から準備すること

```
✅ データ蓄積の仕組みを実装
✅ CSV/SQLite に毎日のデータ保存
✅ API ルートの骨組みを用意
✅ ユーザー管理の概念設計

❌ まだ実装不要
  - 決済機能
  - ユーザー認証
  - アナリティクス
  → フェーズ 3-4 で追加
```

---

**これで全 9 個の .md ドキュメント完成です！**

**次のステップ**：
1. これらの .md ファイルをプロジェクトに追加
2. プロジェクト化して実装開始
3. ステップ 1 から始める
